Системы счисления

0B - признак двоичной С/С
0x - признак шестнадцатеричной с/с

String в Java
    класс для строковых переменных
    неизменяемый
    символы хранятся в UTF-16
    конкатенация ОЧЕНЬ ЗАТРАТНАЯ операция (в 500 раз медленнее append() в StringBuilder)

Время
    хранится в мс в отсчете от 00:00 1 января 1970 года (linux эпоха)




    Попробовать Printstream